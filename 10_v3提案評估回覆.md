---
updated: 2026-07-31
tags: [projects, hackathon, hoyabit, architecture, evidence-pipeline, review]
summary: Ken 的 HOYA Research Agent v3（Evidence-driven Research Pipeline）逐 Stage 對照現有程式碼的可行性評估——12 個 Stage 已有 9 個；「移除 Claim」的診斷指錯欄位但精神正確，真正的錨定在 direction_matrix，已於本日修掉
---

# v3 提案評估回覆

> 對照提案：Ken 的「HOYA Research Agent v3 — Evidence-driven Multi-source Crypto Research Framework」（2026-07-31）。
> 對照前一輪：[[09_權重改版草案]]（v2 的權重部分）與 `requirements.md` R8 ／ `design.md` ADR-8（v2 的採納裁定，2026-07-30）。

## 一句話結論

**v3 的 12 個 Stage，我們已經有 9 個。** 真正缺的是 Knowledge Layer、Evidence Graph、
Hypothesis Generator 三層，其中後兩者在 07-30 的 ADR-8 已裁定賽前不做，理由未變。

但提案裡**有一個判斷是對的而且很重要**——「Debate 之前不該存在任何方向性結論」。
只是它指的位置不對：問題不在 `related_claim`，在 `direction_matrix`。
這一項**今天已經修掉了**（見下方第三節）。

---

## 一、逐 Stage 對照現況

| v3 Stage | 我們的現況 | 對應程式碼 |
|---|---|---|
| Query Understanding（Asset／Horizon／Goal） | ✅ 有，且比提案細 | `detect_coins_in_text()`／`resolve_primary_horizon()`／`classify_question_type()` |
| Research Orchestrator | ✅ 有 | `orchestrator.collect_all()` |
| Collectors | ✅ 有 7 個（提案圖上 6 個） | `agent/collectors/` |
| Stage 1 Evidence Validation | ✅ 有，但散在 filters 層 | 信譽表／新鮮度因子／dedup 交叉驗證／Data Confidence 缺漏檢查 |
| Stage 2 Feature Extraction | 🟡 有算，但輸出是字串 | collector 內算百分位／MA 位置／RSI，寫進 `content_reference` |
| Stage 3 Knowledge Layer | ❌ 沒有 | — |
| Stage 4 Dynamic Weight | 🟡 已是動態，缺 Market Regime | `compute_four_factor_weight()` ＋ `static/signal_importance.json` |
| Stage 5 Evidence Card | 🟡 15 欄裡有 10 欄 | `schemas.Evidence` |
| Stage 6 Evidence Prioritizer | ✅ **07-30 剛做完** | `_evidence_priority()`（R8-4） |
| Stage 7 Evidence Graph | ❌ 沒有（ADR-8 裁定不做） | — |
| Stage 8 Hypothesis Generator | ❌ 沒有（ADR-8 裁定不做） | — |
| Stage 9 Debate | ✅ 有，多輪 Bull/Bear | `build_step_c1/c2_*_prompt()` |
| Stage 10 Judge | ✅ 有 | `build_step_d_prompt()` |
| Stage 11 Confidence Engine | ✅ 有，6 個輸入我們有 5 個 | `compute_confidence()` |
| Stage 12 Research Report | ✅ 有 | `report/builder.py`＋`view_builder.py` |

### 兩點想特別回應

**Time Horizon 那一支我們昨天才剛做完，而且比提案圖上的細。** 提案寫 1D／1W／2W／1M
四檔，我們是五帶（spot／short／medium／long／structural），從題目文字決定性解析、
會在報告揭露判定依據（「因為題目寫了『過去一年』所以主視野是 structural」）。
而且 07-31 剛補上「主視野往下傳給 collector，各自調整查詢窗」——問一年期的題目時
social 會改抓 `t=year` 而不是 `t=week`。這正是提案圖上「Time Horizon 餵進
Orchestrator 再分流到各 collector」那條線。

**「權重不再固定」這點可能有誤會——我們的權重本來就不是固定的。**
現行 `w = 新鮮度 × 來源等級 × 覆蓋度 × dedup_penalty`，其中新鮮度隨執行時間變、
覆蓋度與 dedup 隨當次抓到什麼變，只有來源等級是查表。加上 07-30 新增的
`signal_importance.json`（依 source_type／題型給係數），結構上就是提案說的
`Prior Weight × Dynamic Modifier`。v3 在這塊真正多出來的只有 **Market Regime**
一個 modifier，而那恰好是最貴的一個（見第四節）。

---

## 二、Evidence Card 欄位盤點

提案列了 15 個欄位，我們有 10 個：

| 提案欄位 | 現況 |
|---|---|
| Evidence ID / Category / Fact | ✅ `id` / `source_type` / `content_reference` |
| Evidence Weight / Source Reliability | ✅ `source_weight` / `weight_reason`（含分級與四因子拆解） |
| Primary Horizon | ✅ `horizon_class`（五帶） |
| Persistence / Decay Pattern | ✅ **07-30 剛加**（R8-1，正是採納 v2 的建議） |
| Traceability | ✅ `source_url` / `fetched_at` / `window_start`–`window_end` |
| Features | 🟡 有算但混在 `content_reference` 字串裡，沒有結構化欄位 |
| Knowledge / Historical Support | ❌ 沒有 |
| Evidence Scope | ❌ 沒有 |
| Related Evidence | ❌ 沒有（dedup 有算群組，但沒存成欄位） |
| Failure Conditions | ❌ 沒有（報告層有 `invalidation_conditions`，但那是結論級不是證據級） |

`persistence`／`decay` 是上一輪直接採納 Ken 建議做的，**而且做出來確實有用**：
07-31 用「過去一年」的題目實跑，同一份證據在 medium 主視野下資料品質 100 分、
在 structural 主視野下掉到 80 分，因為 news／onchain／social 的訊號有效期
覆蓋不到一年期的判斷。這個扣分是對的，而且報告會寫出來給讀者看。

---

## 三、關於「移除 Claim」——方向正確，但位置指錯了

這是整份提案我認為最有價值的一點，所以查得比較仔細。

### 我們的 `related_claim` 裡面裝的不是 Claim

我把 07-31 那次真實執行的 33 筆證據全撈出來，25 個不重複的 `related_claim` 值長這樣：

```
BTC 鏈上活躍度（交易量、mempool、算力）
BTC 官方發布新聞事件
BTC 期貨到期結構曲線形狀（contango/backwardation）
BTC 社群討論熱度與情緒
整體加密市場情緒指標（近 30 天百分位）
...
```

**全部是主題標籤，沒有一筆是方向性結論。** 這個欄位名字叫 Claim、內容是 Topic——
命名確實誤導（我猜這就是提案會判斷「我們有 Claim 層」的原因），但它不會造成錨定。

### 真正的錨定在 `direction_matrix`

Step B（交叉驗證層，**在辯論之前**）會讓模型對六類來源各投一次方向票：

```json
"direction_matrix": [
  {"source_type": "price",       "direction": -1, "basis": ["ev-001", ...]},
  {"source_type": "derivatives", "direction":  1, "basis": ["ev-009", ...]},
  {"source_type": "macro",       "direction": -1, "basis": ["ev-022", ...]}
]
```

然後整包 `cross_validation` 被 `json.dumps` 餵進**五個** prompt：正方首輪、
正方反駁輪、反方、反方第二輪、裁判。也就是說**雙方開口之前，就先看到六個
預先算好的方向判決**。

07-31 那次的表是 price=−1（權重 0.92，全場最高）／macro=−1／derivatives=+1／
onchain・news・social=0——淨值偏空，而且最權威的來源已被預先判定看空。
**正方要推翻的不只是反方的論證，還有一份已登記在案的表態。**

這跟另一個長期觀察對得上：`debate_adjustment`（裁判給的辯論後信心調整，範圍 −15~+5）
在**連續五次真實執行中全部為負**（−10／−12／−6／−10／−10）。

### 已修（2026-07-31）

新的不變量：**`direction_matrix` 在 Step B 產生，只給計分層用，不進任何下游 LLM prompt。**

- 拿掉的只有**數值表態**。`consistent_signals`／`contradictions`／`structural_context`
  三段質性內容全部保留——那些是「哪些來源彼此呼應、哪裡真的衝突」，是辯論的原料；
  被移除的僅是「所以偏多還偏空」這個預判。
- **裁判也一併拿掉**，理由不同但同樣具體：`direction_matrix` 已經決定性地算進
  Signal Consensus、占基底分 40%。裁判若再看一次同一份表，同一個訊號會在
  `base` 與 `debate_adjustment` 各計一次分，而後者按設計只該反映辯論品質。
- 計分層完全不受影響，Signal Consensus 照算。

### 但它沒有解決 `debate_adjustment` 全負——這點要說清楚

修完後用**同一題**再跑一次真實執行，`debate_adjustment` **仍是 −10**（連續第六次全負）。

| | 修改前 | 修改後 |
|---|---:|---:|
| `debate_adjustment` | −10 | **−10** |
| Signal Consensus | 56.67 | 75.00 |
| 最終信心 | 57 | 65 |

⚠️ **分數從 57 升到 65 不是這次改動的功勞**，別誤記。那是 Step B 自己吐出了不同的
方向表（4 筆且方向更一致，vs 前次 6 筆分歧），把 Signal Consensus 拉高——
而 Step B 的 prompt 這次沒動，屬 LLM 執行間的自然變異。

裁判兩次給的理由高度一致：「反方 N 項核心批評均成立且正方未有效反駁」。
所以**「辯論後信心調整實際上只是辯論後扣分」這個問題另有原因**，不是錨定造成的。
兩個待驗的候選解釋：

1. **反方結構性多一把武器**（STATUS.md 12 ②）：反方輸出 `critique` ＋ `argument`
   （攻擊＋建構），正方只有 `argument`，全程沒有欄位可以攻擊反方。
2. **可能有一部分根本是正確行為**：測試題目是「BTC 過去一年」，而那一年 BTC 跌了 44%。
   正方要論證「結構比表面看起來更正面」本來就是在跟事實作對，裁判判它站不住腳
   未必是偏誤。**要分辨這兩者，得用一個正方有實料的題目測**——已另跑一次
   「最近一週」（主視野 short）驗證，結果見 STATUS.md。

---

## 四、賽前不做的三項，以及為什麼

### Evidence Graph（Stage 7）與 Hypothesis Generator（Stage 8）

ADR-8（07-30）已裁定不做，理由到今天沒變：兩者都要新增一層＋新的資料結構，
而 Step B 現在已經產出扁平版的關係（一致／矛盾／結構脈絡三段），辯論層 07-28 剛
通過三題型真實驗證。賽前重寫已驗證通過的層，風險與收益不成比例。

另外「Continuation vs Challenge」套在**假設驗證題型**上會很怪——那個題型本來就有
一個明確的待驗證陳述（「市場上有聲音認為 X」），正反方對著 X 辯論是自然的，
硬改成「延續 vs 挑戰」反而失焦。三種題型裡有一種不適用，就不適合當通用骨架。

### Knowledge Layer（Stage 3）

要建一個 factor 知識庫（每個指標的歷史支持度、典型確認時間、常見失效條件、參考文獻），
這是一個獨立專案的規模，不是一個欄位。而且它的價值高度依賴內容品質——沒有回測資料
的情況下人工填，填出來的東西未必比現在的靜態係數表更可信。

**折衷已經做了**：`static/signal_importance.json` 就是 Knowledge Layer 裡「重要性」
那一維的簡化版，介面刻意比照回測版設計，日後要換成回測值時**只換 JSON、不改程式**。

> ⚠️ 那份 JSON 現在的數值是我依 design.md 草擬的暫定值，**還沒跟你對過**。
> 賽前想請你比照 `source_reputation.json` 的 dedup_penalty 曲線校準一次。

### Market Regime（Stage 4 的 Dynamic Modifier）／Evidence Scope

Evidence Scope 這個概念我認同——「這筆證據在什麼條件下適用」確實比塞進 Knowledge
更該獨立成欄位。但提案給的範例欄位裡，`Applicable Horizon` 我們已經有了
（就是 `horizon_class` ＋ `persistence`），真正新的是 `Market Regime`
（Trending／High Volatility／Valuation）與 `Market Structure`
（Derivatives Driven／Spot Dominant）。

問題在 **Market Regime 需要先有一個「現在是趨勢盤還是震盪盤」的判定器**，
而那本身就是一道獨立題目：判錯的話，整個權重系統會被一個沒被驗證過的分類器
牽著走，比不判更危險。

**如果時間允許，我傾向的折衷**是只加 `market_structure`（這個可以從
source_type 決定性推導，不需要判定器），先不做 regime。

---

## 五、總結：採納與不採納

| v3 項目 | 裁定 | 狀態 |
|---|---|---|
| Debate 前不存在方向性結論 | ✅ **採納** | 已修（改 `direction_matrix` 流向） |
| Knowledge／Weight／Time Horizon 三者拆開 | ✅ 概念認同 | Time Horizon 已完成；Weight 結構上已是 Prior×Modifier |
| Evidence Card 統一格式 | ✅ 已有 10/15 欄 | 逐步補，不重寫 |
| Evidence Prioritizer | ✅ 已做 | R8-4（排序而非新增一層） |
| Persistence／Decay | ✅ 已做 | R8-1 |
| Evidence Scope | 🟡 部分 | 建議只加 `market_structure`，不做 regime |
| Feature Extraction 結構化 | 🟡 賽後 | 要改 7 個 collector 的輸出格式 |
| Knowledge Layer | ⬜ 賽後 | 簡化版已落地為 `signal_importance.json` |
| Evidence Graph | ⬜ 賽後 | ADR-8 理由未變 |
| Hypothesis Generator | ⬜ 賽後 | ADR-8 理由未變；且不適用假設驗證題型 |
| Market Regime | ⬜ 賽後 | 判定器本身是獨立題目 |

**判定原則跟上一輪一樣**（ADR-8）：一項提案值不值得賽前做，看它是**加欄位**還是
**換一層**。加欄位的都做了，換一層的都留到賽後。

---

## 六、想請你確認的兩件事

1. **`static/signal_importance.json` 的係數值**——目前是暫定值，需要你校準。
2. **`related_claim` 要不要改名成 `topic`？** 名字確實誤導（誤導到你了），
   但它散在 8 個 collector ＋ 18 個測試檔共 80 處引用，賽前純改名不划算。
   我目前只在 schema docstring 註明「這是主題標籤、不是方向性主張」，賽後再改。
