# 裁判機制實測報告（三題型 × Bedrock 正式後端）

- 執行日期：2026-08-01（UTC+8 12:30–12:50）
- 後端：`LLM_BACKEND=bedrock`，`global.anthropic.claude-opus-4-6-v1` @ `us-west-2`
- 題目：命題文件範例一／二／三逐字，幣種池 BTC/ETH/SOL/BNB/XRP
- 產出：`output/test_q1_multi_source`、`output/test_q2_hypothesis`、`output/test_q3_comparison`
  （⚠️ `output/` 未進版控，這些原始產出只存在當初執行的那台機器上；本報告是唯一
  進版控的記錄，內文引用的數字皆為當時實測值）
- 側錄方式：scratchpad 驅動腳本在 `_call_json_step` 外包一層 tee，把 Step A–D **消毒前**的原始
  JSON 寫進各 run 的 `llm_steps.jsonl`。**repo 程式碼未修改**，走的是 `run_pipeline()` 同一路徑。

---

## 1. 三題執行結果

| | Q1 多源整合 | Q2 假設驗證 | Q3 比較分析 |
|---|---|---|---|
| 幣種 | BTC | ETH | SOL vs XRP（自動偵測） |
| `question_type` | `multi_source` ✅ | `hypothesis_test` ✅ | `comparison` ✅ |
| 牆鐘 | 251.7s | 221.9s | 274.2s |
| `degraded_mode` | False | False | False |
| 證據筆數 | 31 | 30 | 45 |
| LLM 呼叫 | 7 次 / 219s | 7 次 / 189s | 7 次 / 256s |
| 辯論輪數 | 2（`max_rounds`） | 2（`max_rounds`） | 2（`max_rounds`） |
| 信心基底 → 最終 | 82.4 → **79**（中） | 81.0 → **78**（中） | 77.2 → **74**（中） |
| `debate_adjustment` | **−3** | **−3** | **−3** |
| 摘要 raw → 消毒後 | 5 → 5 | 5 → 5 | 5 → 5 |

三題全部在 15 分鐘硬限制內完成（最長 274s，約 30%），沒有任何一題進入 degraded 或 deadline 分支。

---

## 2. 裁判做對的事

### 2.1 守則 #4（反方最後一輪沒人反駁）確實生效

三題的 `debate_summary` 都出現了裁判主動抓反方最後一輪漏洞的條目：

- **Q1**：「反方最後一輪關於 FOMC 的論點（ev-024）存在過度推論：僅因 FOMC 3 天前發布且
  07-31 下跌就推斷政策未提供支撐，但 ev-024 僅記錄日期，無政策內容或市場反應的因果證據。」
- **Q2**：「反方最後一輪稱 RSI14=52.43 位於近 30 天第 10 百分位代表『仍在下滑中』，但
  RSI 52.43 本身處於中性區域……」
- **Q3**：「反方最後一輪第五點（SOL CEX 費率 +10.95%……清算風險更高）有一定道理但被誇大。」

這是整個裁判設計裡最容易失效的一條——反方最後那段沒有對手，只有裁判會看。三題三中。

**三條都回頭對過原始證據，裁判的指正都成立**（純查 `evidence.json`，未用 LLM）：

- Q1 `ev-024` 只有 FOMC 日期欄位，沒有政策內容或市場反應——反方確實是無因果證據的推論。
- Q2 `ev-006` 原文寫「RSI14=52.4（中性區間）……30 天前 48.89 → 現在 52.43（+3.54），橫盤」，
  反方的「仍在下滑中」與證據本身矛盾。
- Q3 `ev-036` 原文寫「CEX 年化費率 +10.95% vs DEX +6.94% → 差 +4.01%（**兩邊費率接近，無明顯場地背離**）」，
  反方拿它推「主流場地槓桿多頭集中、清算風險更高」確實是誇大。

### 2.2 雙向判定，不是一面倒扣分

每題的 5 點摘要都同時包含「反方打中」與「正方擋下」：

- Q1：反方成立 2、正方擋下 1（contango）、反方過度推論 1、打平 1
- Q2：反方成立 2、正方未被推翻 1（催化劑真空）、反方過度推論 1
- Q3：反方成立 2、正方通過壓力測試 1（衍生品深度）、正方雙重標準 1、反方誇大 1

### 2.3 消毒器沒有在背後吃掉東西

三題 `_sanitize_debate_summary()` 都是 5 → 5，引用的 evidence id 全部真實存在，
零幻覺 id。`_clamp_debate_adjustment()` 也完全沒有動作（原始值就是合法 int、落在範圍內）。
**「摘要只有 N 點」在這三個 run 是裁判的真實產出，不是消毒造成的損失。**

---

## 3. 主要發現：`debate_adjustment` 在真實區間內幾乎沒有鑑別力

> ⚠️ **本節描述的是 2026-08-01 修正前的機制。** §9 已把「裁判自報一個整數」換成
> 「逐點判定由程式加總」，本節的 −3/−3/−3 描述的程式碼已不存在。保留本節是因為它是
> §9 那個改動的依據。

三題都回 **−3**。為了分辨這是「三場辯論剛好都值 −3」還是「裁判對這個欄位有固定錨點」，
做了同輸入重打與三組敏感度變體（都以 Q1 的真實 Step A/B 輸出為底；重建的 prompt
與側錄的 `prompt_chars` 逐字元對帳一致，確認重建無誤才發問）。合計 13 次裁判呼叫。

### 3.1 同輸入重打

| 次數 | `debate_adjustment` |
|---|---|
| 原始 + 4 次重打 | **−3, −3, −3, −3, −3**（5/5） |

理由文字每次都不同（59–87 字，措辭各異），但數值完全不動。

### 3.2 敏感度探針

| 變體 | 內容 | 結果 |
|---|---|---|
| `full` | 原樣兩輪 | −3, −3, −3, −3, −3 |
| `r1` | 只給第一輪（批評量減半） | **−3, −3** |
| `one` | 兩輪都保留，但每輪批評只留第 1 點（原文逐字，evidence id 與論證不動） | **−3, −3** |
| `weak` | 反方批評換成無證據、無 evidence id 的空話 | **+3, +3** |

**結論：機制方向是對的，鑑別力是壞的。**

- 好消息：`weak` 變體給出 **+3**，理由寫「反方全程無 evidence id、無具體數據，正方核心論點
  未受有效挑戰，通過壓力測試」。守則 #1「正方擋下就該給正值」**不是死條文**，
  裁判在辯論品質真的塌掉時會翻正號。
- 壞消息：只要反方是一個正常運作的反方（有證據、有具體批評），輸出就固定收斂到 −3——
  換題目（三種題型、四種幣種）不動，換輪數（1 輪 vs 2 輪）不動，
  **把「打中兩項」削成「打中一項」也不動**，同輸入重打不動。

**±3 是一個「中等調整」的語意代號，不是嚴重程度的刻度。** 值得注意的是負向上限是 −15、
正向上限是 +5，而裁判在「反方打中兩項」與「反方全程空話」兩種天差地遠的情境下，
分別給 −3 和 +3——兩個方向都停在 3。它輸出的不是「這場辯論值多少分」，
而是「要調一點點」這個念頭本身，方向對、幅度沒有解析度。

也就是說，**在系統實際運作的區間裡，`debate_adjustment` 是常數 −3**。
`Final = Base + 辯論調整` 這條公式中，區分不同 run 的工作 100% 由決定性的 Base 完成
（82.4 / 81.0 / 77.2），辯論調整只是替所有報告一律扣 3 分。

這對 ADR-5 的設計意圖有直接影響：分層的目的是讓「辯論真正影響分數」，
但目前辯論影響的是一個所有 run 共用的偏移量，不是這一場辯論的品質。

### 3.3 可考慮的方向（未實作）

`-15 ~ +5` 這個範圍配上「小幅／大幅」的自然語言描述，讓模型只能在
「小幅下修」這個語意錨點附近作答。若要讓它有鑑別力，比較可能奏效的是把單一數字
改成**由裁判逐項判定、再由程式加總**——例如每條 `debate_summary` 附一個
`{−3, −1, 0, +1}` 的判定，程式加總後夾值。這樣分數來自可稽核的逐項判定，
而不是模型對一個抽象數字的整體感覺，同時也保留了 ADR-5「可解釋」的訴求。

---

## 4. 其他實測到的缺陷

| 問題 | 證據 | 嚴重度 |
|---|---|---|
| `debate_adjustment_reason` 超過 prompt 明訂的 80 字上限 | Q1 實測 87 字（Q2 53、Q3 64） | 低——`_table_safe()` 在呈現層擋住了，表格不會爆版，完整理由另列。但這說明字數限制只靠 prompt 是不可靠的，防守只有呈現層一道 |
| 面板④ 沒有帶到辯論重點摘要 | 三題 `report_view.json` 的 `panel4_report` 都沒有 `debate_summary` key，`report.md` 有 | 中——與 commit `a668830` 記錄的待辦一致，本次實測確認 |
| `converged` 停止條件很少觸發 | 三題六輪反方全部回 `has_new_points=True`，三題都以 `max_rounds` 收尾 | 低——反方被要求只管建構己方論證，等於要它自認沒新東西可講。**⚠ 後續修正：§9.5 的第四次 BTC 實跑第 2 輪回了 `False`、`stopped_reason=converged`，所以這條分支會觸發，只是很少（累計 1/8 輪）。原本寫「實務上不可達」是三題樣本下的過度推論** |
| social collector 吃 Reddit 429 限流 | Q1 執行紀錄 `social_collector.reddit_CryptoCurrency: 429 限流，等待 20s 後重試` | 低——`.env` 未設 `REDDIT_CLIENT_ID`/`SECRET`，走免 key RSS 端點。三題 social 仍達完整門檻，但這是白白花掉的 20s |

### 反方篇幅天然大於正方

反方每輪「批評＋論證」兩段、正方一段，最後一輪字數比：

| | 正方 | 反方 | 比 |
|---|---|---|---|
| Q1 | 1145 | 1608 | 1.40× |
| Q2 | 1142 | 1263 | 1.11× |
| Q3 | 1292 | 2058 | 1.59× |

prompt 的篇幅條款（守則 #3）有明講這件事。但由於調整值在整個真實區間都是常數 −3，
**本次測試無法檢驗篇幅條款是否有效**——不論篇幅比是 1.11× 還是 1.59×，輸出都一樣，
這個測試對篇幅偏誤沒有鑑別力，既不能證明存在也不能證明不存在。

---

## 5. 本次測試沒有覆蓋到的路徑

三題都走完整辯論路徑，因此**下列最近修改的程式碼未被驗證**：

- `_has_debate_transcript() == False` 的 fallback 路徑（commit `8a30734`、`a668830`
  修的「無辯論時禁止虛構攻防、`debate_summary` 強制空陣列」）
- `stopped_reason` 的 `deadline` / `bull_failed` / `bear_failed` 分支
- `step_c_inference_fallback`（正方首輪失敗時整段退回單模型）

要驗這些需要強制失敗的 run，不能靠正常執行碰到。

---

## 6. STATUS.md 既有疑點結案（用本次真實輸出）

STATUS.md 待辦 9／12 明講「①②④⑤至今全部只有結構推論、沒有輸出實證」。以下逐條用實測回答。

### 6.1 待辦 12③（裁判獎懲對稱化）：**結案，修正有效**

`af3cd3e` 的修正只驗證過「字串存在」，tasks.md 9.7 的反向檢查條件是「正值到底會不會出現」。

| 版本 | `debate_adjustment` |
|---|---|
| 修正前四次真實跑（STATUS.md 記錄） | −10 / −12 / −6 / −10 ← 全負 |
| 修正後本次三題 | −3 / −3 / −3 ← 仍全負 |
| 敏感度探針（反方換成無證據空話） | **+3 / +3 ← 正值確實會出現** |

**9.7 的驗收條件成立**：正值不是死條文，反方品質塌掉時裁判會翻正號。
但注意幅度從 −10 級距整體收縮到固定 −3（原因見 §3）。

### 6.2 待辦 9①／12⑤（收斂權只有反方有、自評有利益衝突）：**疑慮方向錯了**

三題六輪的 `has_new_points` 全部是 `True`，且型別全是 Python `bool`（格式遵從度 100%，
`_coerce_bool` 的寬鬆解析這三題完全沒派上用場）。

- 「反方濫用收斂權提早結束辯論」——**實測不成立，這三題反方一次都沒行使過**。
- 傾向是反向的：反方被要求只管建構己方論證（`DEBATE_ADVOCACY_RULE`），
  卻又被要求自報「我沒新東西了」，這是要它自己認輸，所以它幾乎總是回 `True`。

⚠️ **本節原本寫「`converged` 分支在實務上不可達」，那是三題樣本下的過度推論，已修正。**
§9.5 的第四次 BTC 實跑，反方第 2 輪回了 `has_new_points=False`，
`stopped_reason=converged`——這條分支會觸發，只是很少（累計 8 輪裡 1 次）。
結論改成：收斂權存在且可用，但反方行使意願低，不是「壞掉」而是「幾乎不用」。

### 6.3 待辦 9（第 2 輪正方是真反駁還是換句話說）：**是真反駁**

| | 字元 bigram Jaccard | 字數變化 | evidence id 變化 |
|---|---|---|---|
| Q1 | 0.212 | 1010 → 1145 | +ev-009, +ev-030 / −ev-007, −ev-015, −ev-026 |
| Q2 | 0.191 | 950 → 1142 | +9 筆 / −ev-010 |
| Q3 | 0.233 | 959 → 1292 | +ev-029, +ev-034, +ev-041 / −ev-019 |

重疊率 0.19–0.23，且引用組合有實質增減——第 2 輪是重寫不是改寫。

**順帶修正 STATUS.md 待辦 9① 的一句話**：它寫「可用 `agent/filters/content.py` 既有的
Jaccard 比對前後輪論證強制收斂」。實際上 `content.py` 沒有 Jaccard，函式在
`agent/filters/dedup.py:31`。**而且它不能拿來做這件事**——`jaccard_similarity()` 以空白
切 token，中文整段幾乎切不開，同一組文字它給的是 0.021 / 0.030 / 0.100，
純粹是噪音（對照 bigram 的 0.19–0.23）。要做客觀收斂後備得另外寫中文可用的相似度。

### 6.4 待辦 12①②④⑥（反方結構性優勢）：**有優勢，但不是壓倒性**

把三題 15 條裁判摘要逐條讀完後**人工歸類**（關鍵字比對會誤判，例如 Q2 #4 的
「未被反方有效推翻」出現在句尾，前 40 字看起來像反方得點）：

| | 反方得點 | 正方得點 | 打平／部分成立 |
|---|---|---|---|
| Q1 | 2（#1 #3） | 2（#2 正方擋下、#4 裁判指反方過度推論） | 1（#5） |
| Q2 | 3（#1 #2 #3） | 2（#4 正方站得住腳、#5 反方不夠紮實） | 0 |
| Q3 | 3（#1 #3 #4） | 1（#2 通過壓力測試） | 1（#5 部分成立） |
| **合計** | **8 / 15（53%）** | **5 / 15（33%）** | **2 / 15（13%）** |

反方確實佔優，但裁判沒有橡皮圖章化。這個比例與「調整值固定 −3」是一致的：
反方小幅領先 → 小幅下修。

**沒有支持「反方壓倒正方」的證據**，因此 STATUS.md 建議的下一步 ②
（給正方 `critique` 欄位、動到 debate 契約）**在本次資料上看不出必要性**。
53:33 的落差用一個破壞性的契約變更去修，代價與收益不成比例。
待辦 12 那句「⚠ 仍未回答：隊員看的是哪一次跑的輸出」依然是關鍵——
若隊員看的是 `698a89e`（07-29 10:59）之前的輸出，主因早已移除。

### 6.5 待辦 13（面板④缺辯論重點摘要）：**確認成立**

三題 `report_view.json` 的 `panel4_report` 都沒有 `debate_summary` key，`report.md` 有。

順帶檢查 `pipeline.py:644` 註解擔心的「view_builder 走進 debate 分支讓風險證據消失」：
Q1 面板④ `bullish_evidence=7`／`risk_evidence=10`，沒有發生，這條可以放心。

---

## 7. Code review：裁判機制仍存在的盲點

### 7.1 🔴 裁判被要求「依權重評斷」，卻是唯一拿不到權重表的 prompt

`build_step_d_prompt()`（`prompts.py:719`）明文寫：

> 評估反方批評是否成立時，一併考量雙方所引用證據的權重分佈：以高權重證據支撐的論點，
> 比僅以低權重證據支撐的論點更有份量（低權重證據不足以單獨推翻高權重證據）。

但 `_weight_index_section(evidences)` 只出現在四個地方——`build_step_c_prompt`、
`build_step_c1_bull_prompt`、`build_step_c1_bull_rebuttal_prompt`、`build_step_c2_bear_prompt`
（`prompts.py:404 / 444 / 498 / 559`）。**Step D 沒有。**

另外兩條可能的路徑也都堵死，已逐一查證：

- `facts`：`_sanitize_facts()`（`pipeline.py:272`）只保留 `source_type` / `summary` /
  `evidence_ids` / `coin`，權重欄位被丟掉。
- `cross_validation`：實測 dump Q1／Q3 的 Step B 原始輸出，`weight`／`權重`／`等級`／`A+`
  出現次數**全部為 0**。

也就是說，裁判判斷「誰引用的證據比較有份量」時，**唯一的權重資訊來源是辯士自己在
論證文字裡寫的 `（weight=0.80）`**——而那正是它要裁決的兩造。實測確實看得到這個依賴：
Q1 摘要第 2 點「ev-028（weight=0.80）顯示曲線仍為正向」、Q3 第 3 點「CFTC 數據
（ev-034, ev-041）均為 weight 0.80」，兩處權重都是抄辯士寫的。
只要有一方漏寫或寫錯權重，裁判沒有任何辦法察覺。

**修法**：`build_step_d_prompt` 加上 `evidences` 參數並插入 `_weight_index_section(evidences)`。
這是純加欄位、不動契約的改動，四個辯論 prompt 已經有現成寫法可抄。

### 7.2 🟠 信心分數與信心等級是兩套獨立系統，沒有任何一致性檢查

報告第一行印的是 `信心：79%（中）`：

- `79` 來自 `compute_confidence()` 的決定性公式（`confidence.py:253`），夾在 `[5, 95]`。
- `中` 來自 LLM 在 Step D 自報的 `confidence` 欄位，`builder.py:53 / 215 / 264` 直接取用。

`agent/` 全域搜尋不到任何把兩者對照的程式。理論上可以印出 `信心：92%（低）` 而沒有任何
東西會示警。

**這不是理論風險**——敏感度探針提供了直接證據：把反方換成無證據的空話後，
裁判的 `debate_adjustment` 從 −3 翻到 **+3**（它自己寫「正方論證通過壓力測試」），
但同一次輸出的 `confidence` **仍然是「中」，一動也沒動**。
分數會對辯論品質反應，等級不會。兩者掛在同一行卻各說各話。

**修法（擇一）**：(a) 等級直接由分數決定性映射，刪掉 LLM 那個欄位；
(b) 保留 LLM 等級但加一致性檢查，落差過大時在報告揭露。
(a) 與 ADR-5「可複現」的訴求一致，且 `confidence.py` 檔頭已經寫過舊制
「base 來自 LLM 自報的高/中/低」是被刻意拿掉的東西——等級欄位是那套舊制的殘留。

### 7.3 🟠 摘要點可以完全沒有 evidence id 而不留痕跡

`_sanitize_debate_summary()`（`pipeline.py:290`）的過濾條件只有「`point` 是非空字串」。
`evidence_ids` 經 `_sanitize_ids()` 濾掉幻覺 id 後**就算變成空陣列，該點照樣保留**。
到了 `builder.py:141` 的渲染，`suffix = f"（{...}）" if ids else ""`——
**沒有 id 的點就是不加括號，跟有 id 的點長得一樣**，讀者無從分辨。

`_debate_summary_schema()` 的 docstring 自己就寫過「`_sanitize_debate_summary()`
只濾掉不存在的 evidence id，不會擋掉編出來的 point 字串，所以這裡放行就沒有第二道防線」。
那句話講的是 fallback 路徑的格式範例，但同一個缺口在正常路徑一樣存在。

本次三題 15 點全部帶有效 id，沒有觸發。屬於**沒有防線但這次沒踩到**。

**修法**：渲染時對 `evidence_ids` 為空的點加標記（例如「（未附證據）」），
或在消毒階段直接丟棄無 id 的點。前者較保守，符合 R6-1 降級優先。

### 7.4 🟡 辯論輪之間的時間預算係數 1.5×，實測需要 1.7–2.1×

`pipeline.py:554-566` 的註解說明這個係數的用意是「一輪要花 2 次呼叫（正方＋反方），
裁判還需要 1 次，所以要再開一輪至少得剩下上一輪實測時間 × 1.5」——
也就是 1.5× 要同時涵蓋「第 2 輪」與「裁判」。實測：

| | 第 1 輪 | 第 2 輪 | 裁判 | 實際需要 ÷ 第 1 輪 |
|---|---|---|---|---|
| Q1 | 57.5s | 54.5s | 42.1s | **1.68×** |
| Q2 | 45.6s | 47.9s | 39.4s | **1.91×** |
| Q3 | 56.1s | 60.9s | 56.5s | **2.09×** |

三題全部超過 1.5×。原因很直觀：裁判的 prompt 是全場最長的（Q3 達 18317 字，
因為它要吃完整逐字稿），耗時與辯論的一輪相當，但係數只給了 0.5 輪的餘裕。

**這次沒有觸發**——三題最慢 274s，到 gate 時還剩約 650s，需要的只有約 90s，
差得很遠。這是**潛伏的錯誤校準，不是已發生的故障**，只有在資料蒐集吃掉
900s 中的約 800s 時才進得去那個窗口。

**後果不是「辯論被砍掉」而是「整跑超時」**：`deadline` 只在辯論輪之間檢查一次，
`_call_json_step(..., "step_d_conclusion")` 之前**沒有任何 deadline 檢查**，
而裁判是整條鏈的最後一次呼叫。gate 放行第 2 輪之後，剩下的路一定會走完。
依命題文件執行限制第 1 條，超過 15 分鐘「主辦方可停止執行或不採計超時後產出」。

**修法**：係數從 1.5 提到 2.2（本次樣本上限 2.09 再留一成），
或改成明確的 `last_round_seconds + last_step_d_estimate`。

### 7.5 🟡 推論層 compact 區塊把跨輪 id 掛到最後一輪的論證上

`_format_inference_section()`（`prompts.py:652`）在有辯論時把 `inference` 壓縮成
「40 字截斷的 hypothesis + evidence id 清單」。但那份 id 來自 `_build_debate()` 的
`_union_ids()`——**是所有輪次的聯集**，而 hypothesis 文字是**最後一輪**的。

結果是裁判讀到「[反方] <最後一輪前 40 字>… ← 引用 ev-001…ev-031（含第 1 輪才用過的）」。
第 1 輪引用、第 2 輪已放棄的證據，在裁判眼中仍掛在最後一輪的論證下。
`builder.py:80-92` 的執行摘要**已經修過同一個問題**（明確改用 `rounds[-1]` 的 id，
註解寫「不然會列出文字裡其實沒提到的證據 id」），但裁判 prompt 這條路徑沒有一起修。

實測影響有限（Q1 正方第 1→2 輪移除了 ev-007/ev-015/ev-026，這三筆在裁判眼中仍算正方引用），
但它與 7.1 疊加：裁判既拿不到權重、又拿到錯置的引用歸屬。

### 7.6 ⚪ 觀察（非缺陷）：裁判會引入辯論雙方沒用過的證據

Q3 摘要第 4 點引用了 `ev-018`（XRP 永續負基差），而辯論雙方從頭到尾沒有引用過它。
裁判用它來佐證「ev-038 的訊號本身仍有效」。

prompt 從未限制裁判只能用辯論引用過的 id，而且裁判本來就拿得到事實層與交叉驗證層，
獨立查證正是守則 #4 要求它做的事。15 點裡 1 例，**沒有出現裁判與辯論紀錄相牴觸的情況**。
記錄在此僅供留意「辯論重點摘要」這個容器的語意邊界，不列為缺陷。

---

## 8. 修正優先序建議

| 優先 | 項目 | 改動大小 | 理由 |
|---|---|---|---|
| 1 | §7.1 Step D 補權重索引 | 純加參數，一行 | 裁判正在執行一條它沒有資料可執行的指令，且已實測到它改抄辯士自報值 |
| 2 | §3 `debate_adjustment` 改為逐項判定加總 | 中（prompt + 消毒） | 目前在真實區間是常數，`Final = Base + 調整` 的調整項不帶資訊 |
| 3 | §7.2 信心等級與分數擇一 | 小～中 | 兩套系統掛同一行各說各話，已有實證（探針中等級不動） |
| 4 | §7.4 時間係數 1.5 → 2.2 | 一個常數 | 潛伏但後果是整跑超時、產出可能不被採計 |
| 5 | §7.3 無 id 摘要點加標記 | 小 | 沒有防線，這次沒踩到 |
| 6 | §7.5 compact 區塊改用末輪 id | 小 | 同一個 bug 在 builder 已修過，抄過來即可 |
| — | STATUS.md 待辦 12② 給正方 critique 欄位 | 大（動 debate 契約） | **本次資料不支持**，53:33 的落差不足以支撐破壞性變更 |

---

## 9. 已實作的修正（2026-08-01）

§8 列的六項全數實作，`pytest` 由 560 → 594 全綠。

| # | 項目 | 主要檔案 |
|---|---|---|
| 1 | Step D 補權重索引 | `prompts.py` `build_step_d_prompt(..., evidences=)` |
| 2 | `debate_adjustment` 改逐點判定加總 | `confidence.py` `VERDICT_SCORES` / `tally_debate_verdicts()`、`prompts.py` verdict 說明與 schema、`pipeline.py` `_sanitize_debate_summary()` |
| 3 | 信心等級改由分數決定性推得 | `confidence.py` `confidence_label()`、`builder.py`、`view_builder.py` |
| 4 | 辯論輪時間係數 1.5 → 2.2 | `pipeline.py` `DEBATE_ROUND_TIME_FACTOR` |
| 5 | 無 id 摘要點加標記 | `builder.py` `_build_debate_summary_lines()` |
| 6 | 推論層 compact 改用末輪 id | `prompts.py` `_format_inference_section()` |

### 9.1 逐點判定的級距

| verdict | 分數 | 意義 |
|---|---:|---|
| `bear_valid` | −3 | 反方批評成立且正方未有效回應 |
| `bear_partial` | −1 | 反方批評部分成立／雙方各有道理 |
| `draw` | 0 | 未分出勝負（也是判讀不出來時的落點） |
| `bull_defended` | +1 | 正方擋下批評、論點通過壓力測試 |

`debate_summary` 要求 3–5 點，5 × (−3) = −15 = `DEBATE_ADJUSTMENT_MIN`，
5 × (+1) = +5 = `DEBATE_ADJUSTMENT_MAX`，兩端剛好對齊既有的不對稱範圍。

**降級路徑保留**：一點有效判定都沒有時（fallback 路徑的 `debate_summary` 本來就是 `[]`）
`tally_debate_verdicts()` 回 `None`，退回舊的「裁判自報整數」路徑。回 `None` 而不是 0
是刻意的——回 0 會讓報告寫成「裁判未提供有效的調整值」，讀起來像模型沒答，
但實際上是這次根本沒有辯論。

### 9.2 鑑別力驗算（零額外 LLM 呼叫）

把 §6.4 人工歸類的三題 15 點套進新級距：

| | 判定分布 | 新機制 | 舊機制 |
|---|---|---:|---:|
| Q1 | 反方成立 2、正方擋下 2、打平 1 | **−4** | −3 |
| Q2 | 反方成立 3、正方擋下 2 | **−7** | −3 |
| Q3 | 反方成立 3、正方擋下 1、部分成立 1 | **−9** | −3 |

三題散開成三個不同的值，舊機制是同一個值。

### 9.3 真實 Bedrock 驗證

`pytest` 用的是自己寫的 fixture，抓不到「模型到底會不會照新 schema 回 verdict」——
prompt schema 改動正是模型最容易默默偏掉的地方。所以用 Q1 的真實 Step A/B/C 輸出
重建新版 Step D prompt，實打 Bedrock 兩次：

- **verdict 格式 100% 遵從**，兩次都是 5/5 點附值、全部是 ASCII enum，
  中文別名的容錯路徑沒被用到。
- 加總結果 **−7 / −6**（同一份輸入，舊機制兩次都是 −3）。
- prompt 長度 14752 → 15045 字（+293，權重索引與 verdict 說明）。

### 9.4 這次改動沒有驗到的

- fallback 路徑（無辯論 → `debate_summary=[]` → 退回自報整數）只有單元測試，
  沒有真實跑過。與 §5 列的未覆蓋路徑相同，需要強制失敗的 run。
- 信心等級門檻 80/60 是照「不改變 2026-08-01 三題呈現」選的（79/78/74 → 中/中/中）。
  分數落在門檻附近時的行為，樣本裡沒有。

### 9.5 端到端實跑（改動全部生效）

`output/test_q1_postfix`，BTC 多源整合，265.3s、30 筆證據、`degraded=False`、7 次 LLM 呼叫 233s。

| | 改動前（`test_q1_multi_source`） | 改動後（`test_q1_postfix`） |
|---|---|---|
| `stopped_reason` | `max_rounds` | **`converged`**（反方第 2 輪回 `has_new_points=False`） |
| Step D prompt | 14752 字 | 16040 字（＋權重索引、verdict 說明） |
| 裁判自報 `debate_adjustment` | −3 | −4 |
| **實際採用的調整值** | −3（自報） | **−11（逐點加總）** |
| 逐點判定 | （無此欄位） | `bear_valid` ×3、`bear_partial` ×2 |
| 基底 → 最終 | 82.4 → 79（中） | 77.1 → **66**（中） |

**最值得注意的一點：裁判自報 −4，但它自己的散文理由寫的是「反方在多空比解讀、
Put/Call OI 淡化、鏈上數據過度解讀**三點**批評成立，正方僅在 contango 論點部分守住」。**
三點成立 + 兩點部分成立，照級距就是 −11。**逐點加總與裁判自己的文字一致，
自報的那個整數與它自己的文字不一致**——這正是 §3 說的「模型輸出的是『要調一點點』
這個念頭，不是嚴重程度的刻度」。

報告渲染確認正常：
- 信心分項表格的「辯論後調整」儲存格放的是可驗算的加總式
  `反方批評成立 3 點 × -3；反方部分成立 2 點 × -1`，散文理由完整列在表格下方。
- 「這個分數怎麼來的？」的條列同步帶上加總式。
- 信心等級「中」由分數 66 決定性推得（60–79 區間），與裁判自報的「中」一致，
  所以這次沒有觸發分歧揭露那一行。
- 辯論重點摘要 5 點全數保留、逐字稿維持收合。

**順帶推翻本報告自己的一個結論**：這次 `stopped_reason=converged`，
§4 與 §6.2 原本寫的「`converged` 分支實務上不可達」是三題樣本下的過度推論，已就地修正。
累計 8 輪裡觸發 1 次——會動，只是反方行使意願低。

---

## 10. 過度承認的修正（2026-08-01，第二輪改動）

### 10.1 問題與實測依據

正方第 2 輪普遍先「承認…批評成立」再換一組新證據重講，而不是先檢驗批評成不成立。
直接病因是 `build_step_c1_bull_rebuttal_prompt` 的任務 1 原文：

> 逐項回應反方的批評。批評成立的部分要誠實承認並修正你的論證，**不要硬拗**。

這句話把「批評成立」當成預設框架，又把防守叫做硬拗。

改法：**改寫任務本身**（只加規則會與任務清單打架，模型跟的是任務清單），
改成「先判定成不成立，再三選一回應」——不成立則具體反駁並維持原主張、
部分成立則界定範圍並**收窄**、確實成立才承認並正面修正。
另加正反方共用的 `DEBATE_REBUTTAL_STANDARD`（三項檢驗：針對的是不是你真的講過的主張／
有沒有具體證據而非換個解讀／低權重挑戰高權重時有沒有說明為何不適用），
掛在 C1 反駁輪與 C2 第 2 輪起；C1 第 1 輪、C2 第 1 輪、單模型 fallback 不掛
（沒有前輪批評可判定 / 沒有對手，同 `DEBATE_ADVOCACY_RULE` 的掛載邏輯）。

規則明寫「承認只在批評通過上述檢驗時才給」——要的是先檢驗再決定，不是一律不認。
**教模型拒絕有效批評比過度承認更糟。**

### 10.2 行為變化（BTC 多源整合，同一題連續三跑）

| | 改動前 | 逐點判定版 | ＋判定標準版 |
|---|---|---|---|
| 產出 | `test_q1_multi_source` | `test_q1_postfix` | `test_q1_rebuttal2` |
| 正方 R2 讓步詞 | 10 | **13** | **4** |
| 正方 R2 明確反駁詞 | 0 | 0 | **3**（皆為「不成立」） |
| 逐點判定 | （無此欄位） | `bear_valid`×3、`bear_partial`×2、`bull_defended`×0 | `bear_valid`×2、`bear_partial`×2、**`bull_defended`×1** |

改動後正方第 2 輪逐條標示判定：**部分成立／不成立／部分成立／不成立／不成立**，
5 條批評中具體駁回 3 條、收窄 2 條，沒有任何一條是「承認後換題」。
判定標準的三項檢驗都看得到實際生效：

- 準則 (c) 權重：「ev-002 為 D 級權重（0.45），不足以推翻 ev-006（weight=0.95）所呈現的整體低波動格局」
- 準則 (b) 換解讀不算證據：「反方將多空比解讀為『被套多頭』，但這只是同一數據的另一種解讀，並無額外證據支撐」
- 收窄而非放棄：「我承認『賣壓枯竭』的措辭過強……但我收窄主張為：波動率壓縮代表多空雙方均缺乏 conviction」

**`bear_valid` 沒有歸零（仍有 2 條），這是合格線**——若掉到 0 代表模型被教成拒絕有效批評，
那會是失敗而非成功。

### 10.3 ⚠️ 分數變化不能當作品質證據

新計分是 `bull_defended` +1、`bear_valid` −3。**叫正方少讓步，分數就一定會上升，
這是機制性的，與分析品質無關。** 本次辯論調整 −11 → −7 有一部分純粹來自這個恆等式，
不可解讀為「分析變好了」。基底也從 77.1 變成 71.7（不同時間點的市場快照、
28 vs 30 筆證據），所以最終分數 66 vs 65 兩者之間沒有可比性。

可以說的只有 §10.2 那張表裡的**行為指標**：讓步詞 13→4、明確反駁 0→3、
出現第一個 `bull_defended`。分數不在可說之列。

### 10.4 順帶抓到的兩個問題

**(a) 第 1 輪計時漏算正方（已修）。** 第一次驗證跑的 gate 日誌寫 `needed=70.3s`，
÷ `DEBATE_ROUND_TIME_FACTOR`(2.2) = 31.95s，正好等於反方單獨的 31.9s——
但該輪實際是 bull 20.7 + bear 31.9 = 52.6s。原因是第 1 輪的正方呼叫在辯論迴圈外，
而 `round_started` 設在迴圈內，**第 1 輪只量到反方、低估四成**，而那個值正是
下一輪 deadline gate 的分母。已改為 `round_started = first_round_started if round_no == 1`。

連帶影響：§7.4 算出來的 1.68 / 1.91 / 2.09 是**拿被低估的分母算的**，
所以 2.2 這個係數的校準基礎並不乾淨，等有足夠的乾淨樣本應該回頭重算。

**(b) 🔴 單次 Bedrock 卡頓就會讓整跑超過 15 分鐘（已修，見 §11）。**
第一次驗證跑 `output/test_q1_rebuttal` 花了 **1189 秒**，其中 `step_a_facts`
單一次呼叫卡了 **1020.4 秒**（同題其他跑都在 33s 上下）。

- deadline gate 這次**正確動作**：剩餘 −242.6s → 跳過第 2 輪、把時間留給裁判。
- 但整跑仍然超時，因為 **Step A/B 在任何 deadline 檢查之前**，Step D 之後也沒有檢查。
- `bedrock_client.py` 建 `boto3.client()` 時沒有傳 `Config`，用 botocore 預設，
  外面再包 3 次應用層 exponential backoff——**沒有任何一層有「總時間預算」的概念**。
- 依命題文件執行限制第 1 條，超過 15 分鐘主辦方可停止執行或不採計超時後產出。

建議修法：給 boto3 client 傳 `Config(read_timeout=..., retries={...})`，
並把剩餘預算傳進 `_call_json_step`，讓每次呼叫都受同一個總預算約束。

---

## 11. Bedrock 卡頓事故與修正（2026-08-01）

### 11.1 為什麼「之前都五分鐘內」，這次突然 1189 秒

不是模型變慢，是**餘裕本來就只有 6%，這次剛好用完**。

`boto3.client()` 不傳 `Config` 時 `read_timeout` 預設 **60 秒**。而 27 次真實呼叫的
樣本裡，各步驟的最慢值是：

| 步驟 | n | 平均 | 最慢 |
|---|---:|---:|---:|
| step_a_facts | 5 | 34.6s | 43.3s |
| step_b_cross_validation | 6 | 33.5s | 39.4s |
| step_c1_bull_r1 / r2 | 6 / 5 | 20.9s / 22.7s | 23.3s / 24.5s |
| step_c2_bear_r1 / r2 | 6 / 5 | 33.5s / 31.5s | 37.4s / 36.4s |
| **step_d_conclusion** | 6 | 48.0s | **56.5s** |

**56.5s vs 60s 的上限——只剩 6% 餘裕。** 系統一直在懸崖邊上跑，
「都在五分鐘內」只是還沒踩空。

踩空之後為什麼是 17 分鐘而不是 60 秒——因為重試被疊了兩層（皆已查證）：

1. botocore 預設 `retries={'mode': 'legacy'}`，`__default__.max_attempts = 5`；
   `general_socket_errors` 政策的 `EXCEPTION_MAP` **包含 `ReadTimeoutError`**。
   → 單一次 `client.converse()` 內部最多重打 5 次，各等 60 秒。
2. `ReadTimeoutError` 是 `BotoCoreError` 的子類，會被 `bedrock_client` 外層的
   `except (ClientError, BotoCoreError)` 接住，再重試 3 次。

兩層相乘最多 15 次 × 60s ≈ 900s ＋ backoff，與實測的 1020s 相符。
**而且全程沒有留下任何一行 log**，事後只能靠側錄的耗時反推。

### 11.2 修正

| 對策 | 內容 |
|---|---|
| 拉高 read_timeout | 60s → **150s**（實測最大值的 2.7 倍），連線逾時 10s；兩者可用 `LLM_READ_TIMEOUT_SECONDS` / `LLM_CONNECT_TIMEOUT_SECONDS` 覆寫 |
| 關掉 botocore 重試 | `retries={"mode": "standard", "max_attempts": 0}`，重試政策只留應用層一份。**注意 botocore 的 `max_attempts` 算的是「重試次數」不是「總嘗試次數」，0 才是完全不重試**（解析後 `total_max_attempts=1`；填 1 會變成 2 次） |
| 重試留下紀錄 | `BedrockClient.on_retry` callback，由 orchestrator 接到 `execution_log.jsonl`（`action="llm_retry"`） |
| 感知總時間預算 | `BedrockClient.deadline` 由 orchestrator 設為與辯論 gate 同一個硬性 deadline。每次嘗試前檢查；剩餘時間短於下一次 backoff 就不再重試，把時間留給下游 |

代價：throttling 的重試也只剩應用層的 3 次。可接受——因為現在看得見，
而且底層那層本來就無法記錄、也感知不到總預算。

`GeminiClient` 未動：開發階段暫代後端，正式比賽不得使用，改它只會擴大 diff。

### 11.3 驗證範圍

- `pytest` 607 全綠，含 5 個新的邊界測試（read_timeout 餘裕、botocore 重試已關、
  預算耗盡時提前失敗、重試有紀錄、預算短於 backoff 時不重試）。
- `scripts/check_llm.py` 實打 Bedrock 確認新的 `Config` 沒有弄壞正常路徑。
- **卡頓路徑本身無法按需重現**，所以沒有實跑驗證。上面測的是「設定值是不是我以為的那樣」
  與「預算邏輯的分支」，不是「真的遇到 ReadTimeout 會怎樣」。

---

## 12. 15 分鐘硬性上限：從「盡量不超時」改成「不可能超時」（2026-08-01）

§11 把單次呼叫的 read timeout 收到 150s、關掉 botocore 重試，解決了 1020 秒卡頓的
成因。但那還不是保證：三步各重試 3 次 × 150s 疊起來仍然可以超過 900 秒。
這一節把它補成硬保證。

### 12.1 兩層保證，缺一不可

| 層 | 保證 | 位置 |
|---|---|---|
| **不啟動** | 剩餘時間不足以完成一次呼叫時，直接放棄該步驟，不發新請求 | `pipeline._call_json_step()` 的 `MIN_LLM_STEP_SECONDS`（20s）門檻 |
| **不超時** | 已經發出的請求也不能跑過截止——依剩餘預算收緊該次的 socket read timeout | `BedrockClient._effective_timeout()`：`min(設定值, 剩餘 − connect_timeout − 安全邊際)` |

**只有第一層是不夠的**：在截止前 1 秒發出的請求，照樣能跑滿 150 秒的 read timeout
才回來。botocore 的 timeout 綁在 client 建構時、無法逐次指定，所以改成依 timeout 值
快取多個 client。

### 12.2 其餘保留與門檻

- **報告收尾預留 20s**（`orchestrator.REPORT_RESERVE_SECONDS`）：推理層拿到的 deadline
  比真正的截止早 20 秒，留給報告組裝、寫檔與引用檢查。這些都是本機運算、實測 1 秒內，
  但硬性上限不能剛好用滿。
- **裁判預留 120s**（`pipeline.STEP_D_RESERVE_SECONDS`，實測 step_d 最慢 56.5s 的約 2 倍）：
  進辯論前先確認「一輪辯論＋裁判」的時間留得住，不夠就**整段跳過辯論**直接進結論。
  沒有結論的辯論沒有價值；這條路徑連單模型 fallback 都不發（那也是一次呼叫）。
- **重試也受同一個預算約束**：JSON 解析失敗的那次重試、以及 `BedrockClient` 的
  exponential backoff，剩餘時間短於下一次 backoff 就不再重試。

### 12.3 契約變更

`test_deadline_stops_the_debate_before_starting_another_round` 原本用「已經超時」的
deadline，並期望裁判仍然跑完。那與硬性上限不相容——超時後再發呼叫只會讓整跑更晚結束，
而超時後的產出主辦方可以不採計。**現在超時就完全不發 LLM 呼叫**，由 orchestrator 既有的
`except ReasoningStepError` 走誠實揭露的降級報告。原測試已改寫，另加
`test_expired_deadline_makes_no_llm_call` 明確鎖住新契約。

### 12.4 全流程的時間邊界盤點

| 階段 | 邊界 | 依據 |
|---|---|---|
| 資料蒐集 | 約 75s | 每個 collector 都包在 `asyncio.wait_for(timeout=COLLECTOR_TIMEOUT_SECONDS)`，且全部平行 |
| 去重／話術／權重／注入過濾 | 本機運算，實測 <1s | 無網路呼叫 |
| Step A–D | 每次呼叫雙層設限，且預算不足就不啟動 | §12.1 |
| 未過濾對照組（`--with-baseline`） | 同上——共用同一個 client，因此同樣受 `_effective_timeout()` 約束；失敗時 `run_baseline` 內部已捕捉，回 `{"error": ...}` 不中斷 | `baseline.py:50` |
| 報告生成 | 本機運算，另有 20s 預留 | §12.2 |

### 12.5 驗證

`pytest` 613 全綠，其中直接針對本節的有：

- `test_effective_timeout_never_outlives_the_deadline`：對六個剩餘預算值驗
  `timeout + connect_timeout + 安全邊際 ≤ 剩餘`。
- `test_effective_timeout_is_capped_by_the_configured_value`：預算充裕時不會無限放大。
- `test_no_request_is_sent_when_budget_is_exhausted`
- `test_pipeline_stops_issuing_calls_and_stays_within_budget`：慢速 client ＋ 極小預算，
  驗呼叫次數受限且總耗時在預算內。
- `test_debate_is_skipped_entirely_when_only_the_judge_fits`：只發 A／B／裁判三次呼叫。
- `test_expired_deadline_makes_no_llm_call`

**仍然無法用測試涵蓋的**：真實 `ReadTimeoutError` 的行為（無法按需重現）。
上述測的是設定值與預算邏輯的分支，不是「真的遇到 socket 逾時會怎樣」。

---

## 13. 徹底檢視：裁判機制能否正確回應各種題型（2026-08-01）

問題：這套裁判機制設計得夠完善嗎？能正確回應各種類型的題目嗎？

結論先講：**推理品質本身是好的，裁判的核心職責做得到位；但整套機制是繞著
「多源整合」題型長出來的。** 比較分析題有三個各自獨立的計分失真同時存在，
而題型分類器可能在裁判看到題目之前就把題目路由錯。競賽是隨機抽一題，
風險因此高度集中——抽到比較題時三個失真會一起啟動。

以下每一條都附實測依據，未經驗證的推論會明講。

### 13.1 做得好的部分（已驗證）

| | 依據 |
|---|---|
| 裁判守則 #4（反方最後一輪沒人反駁）確實生效 | 三題三中，且三條指正都回頭對過 `evidence.json` 原文，全部成立（§2.1） |
| 不是橡皮圖章 | 15 條摘要人工歸類 反方 8／正方 5／打平 2（§6.4） |
| 消毒器不會默默吃掉內容 | 三題 `debate_summary` 皆 5→5，零幻覺 id（§2.3） |
| 逐點判定有鑑別力 | 舊機制三題全 −3；新級距套用後為 −4／−7／−9（§9.2） |
| 辯士不再過度承認 | 讓步詞 13→4、明確反駁 0→3，且 `bear_valid` 未歸零（§10.2） |

### 13.2 🔴 最大風險：題型分類器可能在裁判之前就出錯

`classify_question_type()` 是關鍵字比對、**先比中先贏**，而字典順序是
comparison → hypothesis_test → multi_source。實測：

| 題目 | 應為 | 實際 | 後果 |
|---|---|---|---|
| 「**SOL 與 XRP 何者**在當前宏觀環境下風險敞口較高？」 | comparison | **multi_source** | 🔴 `coin2` 偵測只在 comparison 分支執行 → **只蒐集單一幣種證據去回答雙幣種題目** |
| 「分析 BTC 當前市場狀況，說明各類資料**是否**一致」 | multi_source | **hypothesis_test** | 正反方改成驗證一個不存在的陳述 |
| 「分析 SOL 當前狀況，說明鏈上活躍度**相較**上月的變化」 | multi_source | **comparison** | 🔴 觸發 coin2 偵測與雙幣種平行蒐集 |
| 「有人**主張** BNB 已經見底，請評估這個說法站不站得住腳」 | hypothesis_test | **multi_source** | 退回通用框架，失去「明確蒐集支持與反對」的指示 |
| 「市場上有聲音認為 XRP 將補漲，請**比較**支持與反對的證據」 | hypothesis_test | **comparison** | 先比中 comparison |

命題文件明說「正式題目涵蓋但不限於以下範例題型」，實際題目是現場公布的。
**分類器只認得範例題目的措辭**，換個說法就可能錯。第 1 列最嚴重——
它會讓系統拿單一幣種的證據去回答比較題，而這個錯誤發生在資料蒐集階段，
裁判再強也救不回來。

### 13.3 裁判 prompt 幾乎不隨題型改變

實測三種題型的 Step D prompt 長度為 2929／2930／2953 字，**逐行 diff 只差一行**
（題型 framing 那句）。裁判守則、verdict 定義、`debate_adjustment` 語意、
輸出 schema 全部相同。

這本身不必然是問題（推理骨架共用是刻意設計），但它意味著
**下面 13.4–13.6 的題型專屬問題沒有任何一處會被 prompt 層攔下**。

### 13.4 🔴 不對稱級距對「比較分析」題在概念上不成立

`VERDICT_SCORES` 的 `bear_valid=−3` / `bull_defended=+1`，不對稱的理由（ADR-5）是
「辯論若揭露實質漏洞應大幅下修，若只是確認原有判斷最多小幅上調」——
**這個理由預設了「反方＝質疑者」**。多源整合與假設驗證確實如此。

但比較題的角色是：

```
正方: 你要建構「SOL 相對於 XRP 更值得優先關注」的論證。
反方: 你要建構「XRP 相對於 SOL 更值得優先關注」的論證。
```

**兩邊都是倡議者，沒有質疑者。** 於是「XRP 方得一分」被扣 3 分、
「SOL 方守住一分」只加 1 分——分數取決於**主辦方先寫哪個幣**。

用 Q3 的實際判定（反方 3／正方 1／部分成立 1）試算：

| | 辯論調整 |
|---|---:|
| 現況（主幣＝SOL） | **−9** |
| 若題目寫成「比較 XRP 與 SOL」（同一份市場事實） | **−1** |

**8 分的落差純粹來自題目的語序。** 兩者都在 `[−15, +5]` 內，沒有被夾值遮掉。

### 13.5 🟠 verdict 級距把兩件不同的事混為一談

`debate_adjustment` 的 prompt 定義是「你對**這份分析報告本身**的信心調整」，
但 verdict 級距量的是「誰贏了這一點」。這兩者在假設驗證題會分岔：

- **反方指出正方推理有瑕疵**（選擇性引用、過度解讀）→ 這是分析品質問題，**該扣分**。
- **反方以證據證明題目的假設為假** → 這是一個**明確的發現**，不是報告不可靠。

Q2 兩種都出現，而且都記成 `bear_valid`：

> #2「正方只引用波動率部分屬選擇性引用」← 前者，該扣
> #1「正方以 14 日淨變動論證盤整，反方批評 14 日振幅 7.7% 非窄幅橫盤」← 後者

而 Q2 的最終判斷其實相當紮實、也給了明確答案：

> 「維持盤整」的說法描述了近期已發生的事實，但作為前瞻判斷其可靠性正在下降……
> 不宜簡單認定 ETH 將繼續無方向橫盤。

假設驗證題答「這個說法不成立」本來就是合格答案，卻因為反方贏得多而被下修。

### 13.6 🟠 比較題的兩個計分失真（皆已實測確認）

**(a) `direction_matrix` 把兩個幣種壓成同一列。**
`_sanitize_direction_matrix()` 以 `source_type` 去重，Step B 的 prompt 也沒有要求
分幣種表態。Q3（45 筆證據、雙幣種）實測**恰好 6 列**，且 `basis` 橫跨兩幣
（price 一列的 basis 有 10 筆）。

`signal_consensus` 佔基底 40%，而它在比較題量到的是「SOL 和 XRP 是不是一起看空」，
**不是「兩者有何差異」——比較這個維度對分數完全不可見。**

**(b) 資料完整度門檻不分幣種。**
`DATA_COMPLETENESS_THRESHOLD` 是全域筆數，兩幣種的證據直接相加：

| | Q1（單幣） | Q3（雙幣） |
|---|---|---|
| onchain | 1 筆 → tier 0.6 | 2 筆（每幣 1 筆）→ **tier 1.0** |
| social | 5 筆 → 1.0 | 3 筆（每幣 1.5 筆）→ **tier 1.0** |
| 資料品質 | 93.3 | 93.3 |

同樣是「每個幣種 1 筆鏈上證據」，單幣題判 0.6、雙幣題判 1.0。
若門檻改為 per-coin（macro 除外，它本來就只跑主幣一次），Q3 的資料品質會是
**80.0 而非 93.3**——基底約高估 5 分。

### 13.7 ⚠️ 覆蓋缺口：新機制只在多源整合題跑過

**這是本節最該立即處理的一條。**

逐點判定與判定標準（`35b0939`）是在 Q1（BTC／多源整合）上開發與驗證的。
Q2、Q3 的產出是**改動前的舊機制**跑出來的——§9.2 那張「三題散開成 −4／−7／−9」
的表是拿人工歸類**試算**的，不是模型實際吐出的 verdict。

也就是說：**假設驗證題與比較分析題，從來沒有真的跑過新的 verdict schema。**
模型會不會在這兩種題型下照樣回合法 verdict、會不會因為比較題沒有「質疑者」
而系統性偏向某一側，目前**沒有任何實測資料**。

### 13.8 綜合評估與建議順序

| 題型 | 現況 |
|---|---|
| 多源整合 | ✅ 設計與驗證都最完整，機制是繞著它長出來的 |
| 假設驗證 | 🟠 骨架適用，但 verdict 語意有 13.5 的混淆；新機制未實跑 |
| 比較分析 | 🔴 三個獨立失真同時存在（13.4 語序決定分數／13.6a 比較維度不進計分／13.6b 資料門檻虛高）；新機制未實跑 |
| 其他未知題型 | 落回 multi_source，是合理的預設，但 13.2 的分類器可能先出錯 |

**建議順序**（前兩項零程式風險，我會先做這兩項）：

1. **用新機制重跑 Q2／Q3**——這是驗證不是改動，補上 13.7 的缺口。
   在確認模型於這兩種題型下的 verdict 行為之前，其餘改動都是盲改。
2. **分類器加一條幣種數量覆寫**：題目文字中出現 2 個以上幣種池成員 →
   直接判 comparison（`detect_coins_in_text()` 已經現成）。這一條就能修掉 13.2
   最嚴重的那列，且與既有關鍵字邏輯不衝突。
3. 比較題的 verdict 級距改對稱（13.4）。
4. 比較題的 `direction_matrix` 改分幣種（13.6a）——動到 Step B schema 與計分層，較大。
5. 資料完整度門檻改 per-coin（13.6b）。

⚠️ **3–5 動到計分核心，且距離競賽已近。** 若時間有限，做完 1、2 並在報告的
「已知限制」誠實揭露其餘三項，比倉促改計分公式安全——命題文件的評分標準
明列「對不確定性與限制的清楚說明」，揭露本身就是得分項。

---

## 14. 補上 §13.7 的覆蓋缺口：Q2／Q3 以新機制實跑（2026-08-01）

§13.7 指出逐點判定與判定標準只在 Q1（多源整合）跑過，Q2／Q3 的產出是舊機制，
§9.2 那張「三題散開」的表是人工歸類**試算**。本節補上真實實跑。

同時也順帶驗證了分類器的幣種數量覆寫（`86e3380`）——兩題的路由與 `coin2` 偵測
（`comparison_coin2_resolved: coin2=XRP`、`relative_metrics_computed`）皆正確。

### 14.1 verdict schema 在兩種新題型都成立

| | 題型 | 幣種 | 牆鐘 | 證據 | verdict 合法率 |
|---|---|---|---|---|---|
| `verify_q2_new` | hypothesis_test | ETH | 245.6s | 24 | **5/5** |
| `verify_q3_new` | comparison | SOL vs XRP | 272.5s | 40 | **5/5** |

全部是 ASCII enum，中文別名的容錯路徑一次都沒用到。**§13.7 的缺口關閉。**

### 14.2 鑑別力是真的（不再是試算）

| 產出 | 題型 | verdict 分布 | 加總 | 裁判自報 |
|---|---|---|---:|---:|
| `test_q1_postfix` | multi_source | valid×3, partial×2 | **−11** | −4 |
| `test_q1_rebuttal2` | multi_source | valid×2, partial×2, defended×1 | **−7** | −3 |
| `test_q1_deadline` | multi_source | valid×2, partial×2, defended×1 | **−7** | −4 |
| `verify_q2_new` | hypothesis_test | **partial×3, defended×2** | **−1** | **+1** |
| `verify_q3_new` | comparison | valid×2, partial×2, defended×1 | **−7** | −3 |

舊機制在真實區間內是常數 −3；現在實測出現 **−1／−7／−11** 三個不同量級。

**Q2 的 −1 是第一次接近零的辯論調整**，而且裁判自報的純量是 **+1**
——**四次真實跑以來第一個正值**（歷史：−10／−12／−6／−10 → −3／−3／−3 → −4 → −3）。
這與 §10 的判定標準改動方向一致：正方守得住的點變多了（`bull_defended×2`）。

裁判自報純量與逐點加總依然系統性背離（Q2 +1 vs −1、Q3 −3 vs −7），
再次確認 §3 的診斷——純量欄位沒有解析度。

### 14.3 🔴 新發現：級距的不對稱不只在幅度，也在**粒度**

```python
VERDICT_SCORES = {"bear_valid": -3, "bear_partial": -1, "draw": 0, "bull_defended": +1}
```

**反方有兩個得分等級（−3／−1），正方只有一個（+1）——沒有 `bull_partial`。**

多源整合與假設驗證題可以自圓其說（反方＝質疑者，質疑的強度本來就該分級）。
但比較題兩邊都是倡議者，於是這套級距**能表達「XRP 方部分成立」，卻無法表達
「SOL 方部分成立」**。

用 Q3 的真實 verdict 試算主幣對調的後果：

| 情境 | 加總 |
|---|---:|
| 實跑（主幣＝SOL） | **−7** |
| 主幣＝XRP，`bear_partial` 無對應等級只能落回 `draw` | **−1** |
| 主幣＝XRP，`bear_partial` 照留（等於「XRP 方部分成立」仍記扣分） | **−3** |

**4–6 分的落差，純粹來自主辦方在題目裡先寫哪個幣。**
這比 §13.4 用人工歸類估的 8 分小，但方向與成因完全一致，而且現在是實測 verdict。

### 14.4 §13.5 的疑慮在本次未重現（但不能算排除）

§13.5 擔心 verdict 把「反方指出正方推理瑕疵」（該扣）與「反方證明假設為假」
（是發現、不該扣）混為一談。Q2 這次的 5 點**全部屬於前者**（選擇性淡化、
統計幻覺之爭、均線 vs RSI 的解讀、突破時間框架、因果證據不足），
判定看起來是恰當的。

而且 Q2 的最終判斷仍然明確且有據：

> ETH 短期（未來 1-2 週）大概率維持盤整、缺乏明確方向性突破，但盤整重心存在
> 向下偏移的傾向……（列出四項支持依據與衍生品面的反向訊號）

**本次沒有出現混淆，但這只是一個樣本。** 這條的成因是級距定義本身沒有區分
這兩種情況，而不是模型這次判錯了——結構性風險仍在，只是這一跑沒踩到。

### 14.5 結論

- 🟢 **§13.7 關閉**：新 schema 在三種題型都能正常運作，verdict 合法率 15/15。
- 🟢 **逐點加總確實有鑑別力**：−1／−7／−11，不再是常數。
- 🟢 **判定標準有效**：Q2 出現 `bull_defended×2`，裁判自報首次轉正。
- 🔴 **§13.4 的比較題不對稱已由實測確認，且比原先診斷更深一層**——
  不只幅度不對稱，連可表達的判定粒度都不對稱（缺 `bull_partial`）。
  若要修，最小改動是為比較題啟用對稱級距
  （新增 `bull_partial: +?`，或比較題改用 `{−2,−1,0,+1,+2}` 這類對稱表），
  而不是只調數值大小。
