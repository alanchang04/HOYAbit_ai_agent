---
updated: 2026-07-28
tags: [projects, hackathon, hoyabit, signals, weights, draft]
summary: 四因子權重公式改版草案——拆成 Layer A(資料品質)／Layer B(市場影響力)，取代 source_weights.py 現有的單層四因子公式；記錄三個發現的結構性問題與 Layer B 未收斂項目
---

# 權重公式改版草案：Layer A + Layer B

> 對照現況：[agent/filters/source_weights.py](agent/filters/source_weights.py)（Alan 於 R12 落地的
> 四因子公式 `w = 新鮮度 × 來源等級 × 覆蓋度 × dedup_penalty`）。
> 本檔只是設計草案，**尚未動程式碼**。

## 為什麼要改

現有四因子公式混了兩件不同的事：「這筆證據乾不乾淨」跟「這筆證據對盤面重不重要」。
盤點出三個結構性問題：

1. **新鮮度在正式執行時恆為 1.0**（[source_weights.py:64](agent/filters/source_weights.py#L64)），只在 dry-run 舊快取才會降級，等於這個因子在賽場上沒在做功，也不該被期待代表「有用」。
2. **覆蓋度對官方新聞結構性卡在 0.7**：news collector 改版後每幣只設一個官方源
   （[news.py:129](agent/collectors/news.py#L129)），去重後獨立來源數永遠 ≤1，湊不到覆蓋度 ≥2 家的門檻，這個因子等於白扣分。
3. **`source_type == news` 判 C 級 0.6 的規則過時**（[source_reputation.json:36](static/source_reputation.json#L36)）：這條規則是新聞還在抓第三方媒體聚合時代設計的，現在 news 已經全部改成官方公告，繼續用 `source_type` 當第一判準會把「官方一手公告」錯判成「未查證媒體」。

## 新公式

```
w = Layer A（資料品質，0~1） × Layer B（市場影響力，查表係數，選配）
```

Layer B 沒有可用資料時視同 ×1（跟現有「選配疊加、接不上就等於×1」的設計原則一致，
見 [[07_流程圖迭代定案]]）。

---

## Layer A：資料品質（定案）

只回答「這筆內容可不可回溯查證、是不是一手」，**不隱含市場重要性**。

| 檔位 | 分數 | 判準 | 對應現況 |
|---|---|---|---|
| 決定性/一手 | 1.0 | 程式化直查交易所/RPC/官方源，非人為轉述 | `source_type ∈ {PRICE, ONCHAIN, DERIVATIVES}` ＋ HOYA BIT 基準CSV/本地技術指標 ＋ news 裡 `source` 字串含「官方源」的證據 |
| 具名機構二手 | 0.85 | 具名可查證的機構聚合/公開指數，非該幣官方直發 | `source_type == MACRO`（Fear&Greed／Frankfurter／FRED）＋ CoinGecko/CryptoCompare 聚合報價 |
| 未查證/匿名 | 0.5 | 無法追溯發言者身分或動機 | `source_type == SOCIAL` |

判準順序：**先查 `source` 字串有沒有官方源標籤，其次才退回 `source_type`**（修正上面
問題3——不能反過來，否則官方新聞會被 source_type=news 誤判）。

沿用機制：跟現有 `_match_level`（[source_weights.py:71](agent/filters/source_weights.py#L71)）一樣的 first-match-wins 表格架構，只是把 `static/source_reputation.json` 的規則表從 8 級改成這 3 級。

其餘現有因子的去留：

- **新鮮度**：保留，但明確定位為「staleness 衰減」，不是品質分數，避免跟 Layer A 混淆
- **dedup_penalty**：保留不變
- **PR 話術降級**：保留不變，且官方數值/官方敘事同樣給 1.0——話術風險已由降級機制處理，Layer A 不重複扣分
- **覆蓋度**：拆出去，`applies_to` 從 `[news, social]` 收斂成只留 `[social]`（修正上面問題2）

---

## Layer B：市場影響力（草案，未收斂）

對應你 07 文件裡機制②（歷史回測相關性）＋機制③（反變異數權重），目前程式碼完全
沒實作，是撿回你自己先前擱置的設計。

```
LayerB(signal_type) = 歷史相關性權重 × 反變異數權重（可選）
```

- **歷史相關性權重**：對每個 signal_type（新聞情緒方向、funding極端值、鏈上巨鯨轉帳…）
  用歷史窗口回測，算它跟「發布後 N 小時報酬方向/幅度」的相關係數，正規化到 0~1，
  賽前算好存檔查表，現場只做乘法
- **反變異數權重**（可選）：`1/歷史波動`，訊號越穩定權重越高
- **存檔路徑**：`static/weights_backtest.json`（統一定案，取代 03 文件裡並存的
  `static/backtest.json` 舊名）

### 未收斂項目（需要你拍板）

1. **outcome 定義**：相關性要拿「發布後固定窗口的報酬方向」還是「幅度」當比對標的？
   決定回測要抓多細的歷史價格資料
2. **缺資料的 signal_type 怎麼辦**：例如剛加的衍生品 collector 還沒有歷史回測資料，
   預設權重=1（不調整）還是先排除該類訊號不進報告
3. **要不要現在做**：03 文件裡你自己寫的優先序是「機制②③是 garnish 不是主菜，
   時間不夠就不接，pipeline 照跑（等於乘1）」——賽前剩餘時間夠不夠做這塊，
   優先序要不要排到 Layer A 之後

---

## 程式碼落點（若定案要動的檔案）

- `agent/filters/source_weights.py`——Layer A 判準邏輯替換 `_match_level`／
  `_coverage_factor`
- `static/source_reputation.json`——規則表改 3 級，判準順序改「先查官方源標籤」
- `static/weights_backtest.json`（新，選配）——Layer B 賽前算好常數
- `tests/test_source_weights.py`——對應改版測項
